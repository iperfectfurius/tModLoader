--- src/TerrariaNetCore/Terraria/GameContent/UI/BigProgressBar/MoonLordProgressBar.cs
+++ src/tModLoader/Terraria/GameContent/UI/BigProgressBar/MoonLordProgressBar.cs
@@ -22,7 +_,7 @@
 
 	public bool ValidateAndCollectNecessaryInfo(ref BigProgressBarInfo info)
 	{
-		if (info.npcIndexToAimAt < 0 || info.npcIndexToAimAt > 200)
+		if (info.npcIndexToAimAt < 0 || info.npcIndexToAimAt > Main.maxNPCs)
 			return false;
 
 		NPC nPC = Main.npc[info.npcIndexToAimAt];
@@ -42,7 +_,7 @@
 		num += _referenceDummy.lifeMax;
 		num += _referenceDummy.lifeMax;
 		float num2 = 0f;
-		for (int i = 0; i < 200; i++) {
+		for (int i = 0; i < Main.maxNPCs; i++) {
 			NPC nPC2 = Main.npc[i];
 			if (nPC2.active && ValidIds.Contains(nPC2.type) && !IsInBadAI(nPC2))
 				num2 += (float)nPC2.life;
@@ -76,7 +_,7 @@
 
 	private bool TryFindingAnotherMoonLordPiece(ref BigProgressBarInfo info)
 	{
-		for (int i = 0; i < 200; i++) {
+		for (int i = 0; i < Main.maxNPCs; i++) {
 			NPC nPC = Main.npc[i];
 			if (nPC.active && ValidIds.Contains(nPC.type) && !IsInBadAI(nPC)) {
 				info.npcIndexToAimAt = i;

--- src/TerrariaNetCore/Terraria/GameContent/UI/BigProgressBar/TwinsBigProgressBar.cs
+++ src/tModLoader/Terraria/GameContent/UI/BigProgressBar/TwinsBigProgressBar.cs
@@ -10,7 +_,7 @@
 
 	public bool ValidateAndCollectNecessaryInfo(ref BigProgressBarInfo info)
 	{
-		if (info.npcIndexToAimAt < 0 || info.npcIndexToAimAt > 200)
+		if (info.npcIndexToAimAt < 0 || info.npcIndexToAimAt > Main.maxNPCs)
 			return false;
 
 		NPC nPC = Main.npc[info.npcIndexToAimAt];
@@ -20,7 +_,7 @@
 		int num = ((nPC.type == 126) ? 125 : 126);
 		int num2 = nPC.lifeMax;
 		int num3 = nPC.life;
-		for (int i = 0; i < 200; i++) {
+		for (int i = 0; i < Main.maxNPCs; i++) {
 			NPC nPC2 = Main.npc[i];
 			if (nPC2.active && nPC2.type == num) {
 				num2 += nPC2.lifeMax;

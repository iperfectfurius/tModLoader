--- src/TerrariaNetCore/Terraria/GameContent/Events/BirthdayParty.cs
+++ src/tModLoader/Terraria/GameContent/Events/BirthdayParty.cs
@@ -77,7 +_,7 @@
 			return;
 
 		List<NPC> list = new List<NPC>();
-		for (int j = 0; j < 200; j++) {
+		for (int j = 0; j < Main.maxNPCs; j++) {
 			NPC nPC = Main.npc[j];
 			if (CanNPCParty(nPC))
 				list.Add(nPC);
